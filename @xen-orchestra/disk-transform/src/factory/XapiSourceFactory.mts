/**
 * implement base mechanism to instantiate a disk from xapi
 * this is currently done by @xen-orchestra/xapi/vdi.mjs
 *
   // prefernbd = false=> stream
    // else
    // got baseref
    // cbt
    // if it fails without cbt
    // fallback to vhdnbd
    // vhdnbd
    // vhdnbd :
    // if base ref && cant do delta
    // fallback to full
*/
