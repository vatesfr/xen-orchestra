export default {
  export: true, // can export the content ( full or file by file )
  read: true, // can read the metadata of an archive
  mount: true, // can mount the archive for file level restore
}
