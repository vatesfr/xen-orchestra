{
  "devDependencies": {
    "husky": "^0.12.0",
    "lerna": "^2.0.0-beta.32"
  },
  "scripts": {
    "commit-msg": "lerna run test",
    "install": "lerna bootstrap",
    "test": "lerna run test"
  }
}
