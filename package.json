{
  "devDependencies": {
    "husky": "^0.14.2",
    "lerna": "^2.0.0-rc.5",
    "promise-toolbox": "^0.9.5"
  },
  "scripts": {
    "commitmsg": "yarn test",
    "install": "lerna bootstrap",
    "test": "lerna run test"
  }
}
