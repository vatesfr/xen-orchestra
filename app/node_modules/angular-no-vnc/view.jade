canvas.center-block(
  height = "{{noVnc.height}}"
  width = "{{noVnc.width}}"
  ng-mouseenter = 'focus()'
  ng-mouseleave = 'unfocus()'
)
  | Sorry, your browser does not support the canvas element.
