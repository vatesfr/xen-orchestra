import pick from 'lodash/fp/pick'
import {
  connect
} from 'react-redux'

import * as actions from '../../store/actions'

// ===================================================================

export const connectStore = (properties) => connect(
  pick(properties),
  actions
)
