import _ from 'messages'
import map from 'lodash/map'
import React from 'react'

export default ({ actions }) => (
  <div>
    {map(actions, ({ label, handler }) => (
      <button type='button' className='btn' onClick={handler}>
        {_(label)}
      </button>
    ))}
  </div>
)
