import React from 'react'
import { propTypes } from 'utils'

export const Col = propTypes({
  size: propTypes.number.isRequired
})(({ children, size }) => <div className={`col-xs-${size}`}>
  {children}
</div>)

export const Row = ({ children }) => <div className='row'>
  {children}
</div>
