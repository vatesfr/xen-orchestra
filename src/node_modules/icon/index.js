import React from 'react'

const Icon = ({ icon = 'error', size = 1 }) => (
  <i className={`xo-icon-${icon} fa-${size}x`}></i>
)
Icon.propTypes = {
  icon: React.PropTypes.string,
  size: React.PropTypes.number
}

export default Icon
